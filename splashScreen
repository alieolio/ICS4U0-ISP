package com.latter.thelatter;

import javafx.animation.FadeTransition;
import javafx.application.Application;
import javafx.fxml.FXMLLoader;
import javafx.scene.*;
import javafx.scene.control.Button;
import javafx.scene.image.Image;
import javafx.scene.image.ImageView;
import javafx.scene.layout.*;
import javafx.scene.text.Font;
import javafx.scene.text.FontWeight;
import javafx.stage.Stage;
import javafx.util.Duration;

import java.io.FileInputStream;

import java.io.FileInputStream;
import java.io.FileNotFoundException;
import java.io.IOException;
import java.util.Timer;

public class splashScreen<Graphics> extends Application {

    private char alpha;
    private boolean fade;
    private Image spsc;
    private ImageView imageSp;
    public splashScreen() throws FileNotFoundException {
        alpha = 0;
        fade = false;

        FileInputStream input = new FileInputStream("splash.png");
        spsc = new Image(input);
        
    }
    
    public void paintComponent (Graphics g) {
        drawImage(spsc);
    }

    private void drawImage(Image image) {
        imageSp = new ImageView(image);
    }
    
    public void play() {
        //Timer time = new Timer(5, this::actionPerformed);
        //time.start();
        
    }

    public void start(Stage primaryStage) throws Exception {

    }
}
